
### Solaar for logitech
pacman -S solaar

# Udev rules
curl https://raw.githubusercontent.com/pwr-Solaar/Solaar/refs/heads/master/rules.d-uinput/42-logitech-unify-permissions.rules > /etc/udev/rules.d-uinput/42-logitech-unify-permissions.rules

# Udev rules
curl https://raw.githubusercontent.com/pwr-Solaar/Solaar/refs/heads/master/rules.d/42-logitech-unify-permissions.rules > /etc/udev/rules.d/42-logitech-unify-permissions.rules

# copy for autostart
curl https://raw.githubusercontent.com/pwr-Solaar/Solaar/refs/heads/master/share/autostart/solaar.desktop > /etc/xdg/autostart/solaar.desktop


### Screeshots grim and slurp
pacman -S grim slurp



# For playerctl work with firefox
pacman -S mpd-mpris

# For media key to work (Not always)
pacman -S xbindkeys

# Waybar Timer
# In waybar config
"custom/timer":{
    //"format":"",
    "exec": "/home/anto/.config/waybar/plugins/timer/waybar_timer hook",
    "exec-on-event": false,
    "return-type": "json",
    "format": "{icon} {0}",
    "format-icons": {
        "standby": "",
        "running": "",
        "paused": ""
    },
    "on-click": "/home/anto/.config/waybar/plugins/timer/waybar_timer new 30 'notify-send \"Session finished\"'",
    "on-click-middle": "/home/anto/.config/waybar/plugins/timer/waybar_timer cancel",
    "on-click-right": "/home/anto/.config/waybar/plugins/timer/waybar_timer togglepause",
    "on-scroll-up": "/home/anto/.config/waybar/plugins/timer/waybar_timer increase 60 || /home/anto/.config/waybar/plugins/timer/waybar_timer new 1 'notify-send -u critical \"Timer expired\"'",
    "on-scroll-down": "/home/anto/.config/waybar/plugins/timer/waybar_timer decrease 60"
}

# Autostart (hyprland ex) : 
exec-once = /home/anto/.config/waybar/plugins/timer/waybar_timer serve

# Hardware accesleration 
pacman -S vdpauinfo vulkan-tools libva-utils libvdpau-va-gl
# Set env (hyprland ex)
env = LIBVA_DRIVER_NAME,vdpau
env = VDPAU_DRIVER,nvidia

# Notification
pacman -S swaync

# Themes (Mocha for catppuccin)
GTK :https://draculatheme.com/gtk
Cursors : https://github.com/catppuccin/cursors
kitty : https://github.com/catppuccin/kitty
VScode : https://github.com/catppuccin/vscode
Vscode icon : https://github.com/catppuccin/vscode-icons
Discord : https://github.com/catppuccin/discord #(For vesktop : /home/anto/.var/app/dev.vencord.Vesktop/config/vesktop/themes/file.css)

# Sudo prompt // TO TEST
pacman -S hyprpolkitagent
exec-once = systemctl --user start hyprpolkitagent

# game utility
pacman -S gamemode lib32-gamemode lib32-nvidia-utils lib32-vulkan-icd-loader mangohud goverlay gamescope

# Settings for vulkan to work better (No effect from what i see | Do not work with nvidia open). In /etc/modprobe.d/nvidia.conf
options nvidia NVreg_EnableGpuFirmware=0

# hyprland plugins :
hyprpm update
hyprpm add https://github.com/hyprwm/hyprland-plugins

# enable hyprbars
hyprpm enable hyprbars

# Thunar file manager + ark file extractor
pacman -S thunar thunar-archive-plugin ark

# Virtualization
sudo pacman -S libvirt virt-manager qemu-base dnsmasq
sudo systemctl enable --now libvirtd.socket
sudo systemctl enable --now virtlogd.socket

# Add : firewall_backend="iptables" to /etc/libvirt/network.conf
# Avec ufw : 
sudo ufw allow in on virbr0

## When need
sudo virsh net-start default

# Infinite bash history size, add to .bashrc
export HISTFILESIZE=
export HISTSIZE=

# Delay after failed login attempt
# Add next line to /etc/pam.d/system-login
auth optional pam_faildelay.so delay=4000000

# Refuse root ssh connection
# Create /etc/ssh/sshd_config.d/20-deny_root.conf and add :
PermitRootLogin no

# Dialogue prompt
sudo pacman -S zenity

# Fallback for xdg desktop portal
pacman -S xdg-desktop-portal-gtk xdg-desktop-portal-wlr

# Backup :
pacman -S borg vorta

# Text editor
pacman -S gnome-text-editor

# Login
pacman -S ly

# Gtk theme manager
pacman -S nwg-look

# Set git editor
git config --global core.editor "nano"
